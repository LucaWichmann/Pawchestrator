Your task: verification or tests failed after implementation.
Fix the failing tests or build errors from the failure output.
Do not re-implement the feature; only repair what is failing.
Make focused commits using `type(scope): description`.
Do not run build | test commands - verification is handled separately.
No commentary. No progress updates. Implement & commit only.
